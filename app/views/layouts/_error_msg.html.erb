<% if model.errors.any? %>
    <div>
      <h4><%= model.errors.count %>件のエラーが発生しました。</h4>
      <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
<% end %>
